# Copy this file to `.env` and update values before running `docker compose up`
# DO NOT commit your real .env with secrets to version control.

# Grafana admin password (change before first use)
GF_SECURITY_ADMIN_PASSWORD=admin

# Pinned image versions (override if needed)
PROMETHEUS_IMAGE=prom/prometheus:v2.49.0
GRAFANA_IMAGE=grafana/grafana:9.5.2
